# Vento Fermo Project

# Setup

```sh
$ mix deps.get
$ yarn # ...or npm install
```

# Development

```sh
$ NODE_OPTIONS=--openssl-legacy-provider mix fermo.live
```

The site is available at `http://localhost:4001`

# Build

```sh
$ NODE_OPTIONS=--openssl-legacy-provider mix fermo.live
```
